<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.accounts.mapper.BookRecordMapper">
    <insert id="insert" parameterType="com.example.accounts.bean.BookRecordBean">
        INSERT INTO book_record(book_id, record_id) VALUES (#{bookId}, #{recordId})
    </insert>
</mapper>